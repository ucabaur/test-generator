{
  "version": "0.2.0",
  "configurations": [{
    "name": "Attach (Remote Debug)",
    "type": "python",
    "request": "attach",
    "localRoot": "${workspaceFolder}/backend",
    "remoteRoot": "/code",
    "port": 9000,
    "secret": "testwillrocks",
    "host": "localhost"
  }]
}
